# 0084 Stage 2 DESIGN — Cross-Sectional Factor Atlas Replacement

Research ID: `BRRK-CRYPTO-CROSS-SECTIONAL-FACTOR-ATLAS-REPLACEMENT-0084`

Lifecycle stage: `2/10 DESIGN`.

Parent Stage1 merge: `a0b4c6b4943850e29ad5392d33be7ab1fb86c65d`.

Governance resolution: `research/governance/CRYPTO_MULTI_STRATEGY_0075_PREMARKER_IMPLEMENTATION_RESOLUTION.md`.

Controlled attempt budget at DESIGN: `0/1`; controlled historical/evidence reads: `0`; scientific engine calls: `0`; scientific source-network fetches: `0`.

## Replacement scope

0084 is the prospectively authorized replacement for 0075 after the pre-marker frozen-implementation defect was resolved by merged governance resolution #381. No 0075 lifecycle credit, historical result, controlled read, attempt consumption, or Stage8 authorization transfers to 0084.

This DESIGN mechanically reproduces the already-frozen 0075 scientific semantics. It may not improve, retune, rescue, widen, narrow, or otherwise alter the scientific question based on any historical scientific value.

## Economic question

Determine whether prospectively defined, point-in-time-valid cross-sectional factor families contain reproducible ranking information across a tradable crypto universe. This stage defines the bounded research architecture only. It does not evaluate factor returns, IC, significance, rankings, winners, or deployable long/short economics.

## Frozen point-in-time universe identity

Stage3 PREREGISTRATION must reproduce the frozen 0075 universe semantics without scientific modification:

- point-in-time eligible ordinary `*USDT` cross-sectional universe;
- anti-lookahead chronology and as-of availability semantics;
- candidate source months `2021-01` through `2026-07`;
- no future survival, later venue availability, revised metadata, or end-of-sample inclusion in historical membership;
- frozen handling of listing age, liquidity/capacity, stale/missing observations, delistings, migrations, redenominations, forks, stablecoins, wrapped duplicates, and non-comparable instruments exactly as specified by the 0075 frozen contract.

## Frozen factor-family ceiling

0084 must bind the same 16 base factors and two prospective representations frozen for 0075. No additional scientific family or factor definition may be introduced under 0084.

The scientific families remain bounded to the 0075 price, risk, market-structure, and point-in-time-valid network/economic-state definitions. Stage3 must reproduce the exact formulas, controls, lookbacks, rebalance cadence, quantile construction, family assignments, trial counts, and multiplicity treatment from the frozen 0075 contract.

## Frozen horizons and trial budget

- matured forward-return horizons: exactly `5` and `20` sessions;
- total declared trials: exactly `64`;
- no uncounted exploratory variant;
- no candidate replacement, family reassignment, threshold rescue, or pruning after controlled history exposure.

## Frozen analysis architecture

Stage3 must preserve the 0075 ordering and semantics:

1. determine the point-in-time universe at rebalance `t`;
2. use only feature values available by `t`;
3. apply the frozen stale/missing filter;
4. apply the frozen winsorization rule;
5. apply the frozen ranking/standardization rule;
6. apply only the prospectively frozen residualization controls and chronology;
7. form the frozen quintile or equivalent cross-sectional buckets;
8. admit forward returns only after full maturity;
9. calculate rank IC, Q5-Q1 spread, and quintile monotonicity;
10. evaluate the frozen time/regime stability partitions;
11. calculate the frozen turnover/capacity diagnostics;
12. apply the frozen family-wise Holm correction with the frozen family counts and alpha;
13. apply the frozen leave-year and leave-size robustness rules and any other already-frozen 0075 robustness partitions;
14. persist every tested factor definition and the full trial count;
15. apply the exact frozen G0-G11 gates and terminal-classification semantics.

## Frozen inference constants

0084 must reproduce the 0075 frozen inference constants exactly:

- moving-block bootstrap block length: `20`;
- bootstrap replicates: `4000`;
- bootstrap seed: `750075`;
- family-wise Holm correction with the frozen 0075 family counts and alpha;
- exact frozen support, regime, leave-year, leave-size, and G0-G11 requirements.

Stage3 may restate these rules for the replacement lineage but may not change them. Any scientific change requires a different prospective research ID before controlled history.

## Stage4 implementation requirement

Unlike the incomplete 0075 implementation, 0084 Stage4 must implement the complete preregistered end-to-end scientific execution interface before qualification. That interface must include deterministic staged-payload parsing and read accounting, every frozen factor calculation, exact 64-trial orchestration, primary statistics, bootstrap and multiplicity inference, all frozen robustness partitions, G0-G11 evaluation, exact counters, and create-only result persistence.

Stage5 must qualify that complete implementation synthetically. Stage6 must pin the exact qualified implementation and allowed source identities. Stage7 must establish zero-result readiness before any Stage8 attempt.

## Terminal meaning

`PASS` means one or more preregistered factor definitions satisfy all frozen atlas robustness, support, multiplicity, and G0-G11 requirements. PASS is structural factor-atlas evidence only.

Only an immutable 0084 PASS closeout may satisfy the prospectively amended prerequisite for 0076, and 0076 may use only factors immutably qualified by the actual 0084 result.

A non-PASS 0084 outcome cannot seed 0076 factor selection. No factor may be inferred, selected, ranked, or carried forward from blocked 0075 because 0075 has no scientific result.

`PASS` does not establish a deployable long/short strategy, portfolio alpha, production authority, or independent OOS evidence.

## Exactly-once and evidence scope

No controlled scientific/history payload may be opened during Stage2. No scientific engine may run. No Stage8 scientific source-network fetch may occur.

If 0084 later reaches Stage8, it requires fresh exact-scope contemporaneous user authorization specific to 0084. The historical 0075 authorization does not transfer.

The later Stage8 attempt must follow this strict order:

1. re-establish live main and identity-only/metadata-only zero-result readiness;
2. create and remotely verify durable `RUN_ATTEMPT.marker` before any controlled content read;
3. consume attempt `1/1` only when marker durability is established;
4. read each authorized historical object at most once;
5. invoke the scientific engine exactly once;
6. perform zero Stage8 scientific source-network fetches;
7. persist the result bundle create-only;
8. seal with final `RUN_ONCE.marker`.

After marker consumption, same-ID rerun, retune, rescue, source substitution, candidate replacement, history extension, or recomputation is forbidden.

Researcher-exposed historical evidence remains DEVELOPMENT history and must never be described as independent OOS.

## Immutable anchors

0070 remains immutable PASS closeout. 0071 remains permanently blocked at 6/10. 0083 remains immutable FAIL closeout at 10/10 with attempt 1/1 consumed. 0072 remains immutable `INCONCLUSIVE_INSUFFICIENT_SUPPORT` at 10/10 with its attempt consumed and no same-ID rerun. 0075 remains permanently `7/10 BLOCKED_PRE_ATTEMPT_FROZEN_IMPLEMENTATION_INCOMPLETE` with attempt `0/1` unconsumed and no scientific result.

CAPTURE-0001 remains sealed failed HTTP 451 with no retry. CAPTURE-0002 remains permanently claimed with no refetch. The exact historical CURRENT_STATE line `workflow run                         31381953131 / attempt 1` remains immutable.

## Production authorization

`production_authorized=false`

`signature_authorized=false`

`order_submission_authorized=false`
