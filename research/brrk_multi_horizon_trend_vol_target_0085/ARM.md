# BRRK Multi-Horizon Trend Vol-Target 0085 — ARM

Gate: `ARM` = `CONTROLLED BOUNDARY` then `ZERO-RESULT PREFLIGHT` under `PROSPECTIVE_FIVE_GATE_LIFECYCLE_V1`.

Status: `ARM_READY_PENDING_EXACT_HEAD_CI`.

## Controlled boundary

The exact controlled source set is the 201 `perpetual_1d_kline` objects for BTCUSDT, ETHUSDT and SOLUSDT from 2021-01 through 2026-07 selected mechanically from the immutable 0074 Stage6 authorized-object manifest. That manifest already binds each selected object to its exact official/staged SHA-256, byte size, month, symbol, staged path and one-read scientific-content budget.

0085 binds the parent manifest by path and content SHA-256 `fba23adb62a1a94492c77e94405593c18cba9226ed20d8c70fda578030d3b23f`. It binds the existing zero-scientific-value staging artifact from workflow run `32590698620`, artifact `9480304574` / `0074-stage6-authorized-payloads-v1`, digest `sha256:dd39591efc19bda3fd959b6eb9f7a78e0f4319ae371b4be9556a505341301c5e`, size `1274724` bytes, expiry `2026-11-20T18:25:37Z`.

Evidence reuse transfers only exact opaque byte identities and durable staging. It transfers no 0074 lifecycle credit, scientific result, attempt, parameter choice or performance claim. 0074 remains immutable `INVALID_EXECUTION`.

The exact controlled execution interface is `research.brrk_multi_horizon_trend_vol_target_0085.controlled_archive_engine.ControlledArchiveTrendEngine`, called through the already-qualified `CONTROLLED_RESEARCH_RUNNER_V1`.

## Zero-result preflight

At ARM construction time, GitHub artifact metadata reports artifact `9480304574` as `expired=false`. The preflight inspected only repository governance/manifest metadata and GitHub artifact identity/expiry metadata. It did not download the artifact, open an authorized payload, decompress a nested ZIP, call `testzip()`, parse CSV, traverse CRC-bearing payload bytes, calculate a Trend signal, or expose any scientific value.

Repository qualification validates the immutable parent manifest mechanically: exactly 201 objects satisfy the frozen kline-only filter, each selected object is unique, every selected row has the required staged/hash/size identity fields and `STAGED_HASH_VERIFIED_OFFLINE_READABLE`, and each scientific-content read budget equals one.

Terminal zero-result preflight classification for this ARM gate is `PREFLIGHT_PASS_ZERO_RESULT_PENDING_EXACT_HEAD_CI`. The classification becomes earned only after the exact ARM head passes all mandatory standing CI and the ARM PR merges.

## Irreversible order after ARM

`RUN` must preserve this order:

1. recheck exact ARM Git/artifact identities using metadata only;
2. create and remotely verify durable `RUN_ATTEMPT.marker`;
3. only then read each authorized outer payload object, at most once;
4. invoke `ControlledArchiveTrendEngine` exactly once;
5. persist create-only result/execution evidence;
6. seal `RUN_ONCE.marker`.

No pre-marker `testzip()`, decompression, inner CSV open/parse, CRC payload traversal, scientific-row read, result calculation or scientific source-network fetch is permitted.

## What did not change

- 0085 frozen Trend science is unchanged: BTC/ETH/SOL, horizons 20/60/120/240, 3-of-4 activation, trailing 20-session realized volatility, 25% annualized portfolio-vol target, gross cap 1.0, long/cash only, UTC daily timing and t-close -> t+1 causality.
- 10/20/30 bps cost panels, support floor, benchmarks, diagnostics, PASS gates and terminal classifications remain unchanged.
- Attempt remains `0/1`; controlled scientific/history reads remain `0`; scientific controlled engine calls remain `0/1`; scientific source-network fetches remain `0`; scientific values exposed remain `false`.
- No `RUN_ATTEMPT.marker`, result bundle or `RUN_ONCE.marker` exists or is authorized by ARM.
- No source substitution, result-informed tuning, threshold rescue, history extension, candidate replacement, rerun, recomputation or 0074 result reuse occurred.
- 0076 remains sealed at its Stage7 pre-marker incident; 0072/0073 Carry remain paused; 0083 remains immutable FAIL.
- `workflow run                         31381953131 / attempt 1`, CAPTURE-0001 sealed/no-retry and CAPTURE-0002 permanently claimed/no-refetch remain unchanged.
- Production/signature/order/withdrawal/transfer authority remains false.

## Next legal step

Merge ARM only after all mandatory exact-head CI reaches terminal green and ancestry/mergeability remain clean. After ARM merge, `RUN` remains a separate irreversible gate. Do not create the attempt marker or read controlled history during ARM.
