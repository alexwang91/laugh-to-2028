# BRRK-BTC-SOL-PATH-EVENT-EARLY-WARNING-RUNTIME-QUALIFIED-0067 — DESIGN FREEZE

Date: 2026-08-14
Stage: `DESIGN ONLY / NO 0067 HISTORICAL MEASUREMENT`
Baseline main: `9a45bb5e778e21dfd30d7abc4ff7d9889542b495`
Research ID: `BRRK-BTC-SOL-PATH-EVENT-EARLY-WARNING-RUNTIME-QUALIFIED-0067`

## 1. Research status and reason for a new ID

0067 is a new research ID. It is not a rerun, rescue, continuation attempt, or marker recovery of 0066.

Closed 0066 is `INVALID_EXECUTION / CLOSED TO SAME-ID RERUN`. Its unique historical attempt was consumed and terminated by the configured GitHub Actions wall-clock timeout before a complete result bundle was durably persisted. Therefore 0066 contributes **process information only**: the complete scientific compute graph was not qualified against the execution envelope before the exactly-once marker was crossed.

0066 contributes no scientific result to 0067. In particular, no 0066 event-atlas result, indicator association, classifier metric, controller metric, CAGR, terminal wealth, MDD, bootstrap result, PBO result, architecture winner, threshold preference, or lead-horizon preference is available for 0067 design selection.

0067 is therefore `PROCESS_INFORMED_BY_0066_TIMEOUT / NOT_RESULT_INFORMED_BY_0066`.

The DEVELOPMENT history remains researcher-exposed history and is not independent OOS.

## 2. Scientific question

The scientific question remains event-first rather than direct-return-first:

1. identify objectively defined BTC and SOL price-path decline and sideways episode onsets;
2. ask whether the complete frozen signal universe contains information that those episodes will begin within short future warning windows;
3. compare several prospectively specified probability-model architectures without privileging a single nonlinear family;
4. translate warning probabilities into prespecified defensive outer gross controllers;
5. compare full-cycle net wealth, drawdown, robustness, and dependence against the closed 0064 passive-cash benchmark.

Scientific priority remains:

1. full-cycle net wealth / CAGR;
2. maximum drawdown and bear-market damage;
3. robustness, costs, and dependence.

No production, signature, or order-submission authority can be granted by 0067.

## 3. Scientific carry-forward policy

The intended scientific semantics are carried forward from the frozen 0066 program rather than redesigned in response to the failed execution.

At DESIGN stage the following scientific structure is preserved:

- outcome assets: BTC and SOL;
- ETH may remain predictor context but not an event-outcome asset;
- price-only event definitions; predictors cannot alter event labels or onsets;
- event classes: DOWN and SIDEWAYS;
- multi-scale future path scan over short through secular horizons;
- duration grades SHORT / MEDIUM / LONG / SECULAR;
- targets per asset: ANY_DOWN / MAJOR_DOWN / ANY_SIDEWAYS / LONG_SIDEWAYS;
- warning leads: 1 / 3 / 5 / 10 / 20 sessions;
- dates inside an already-active event suppression interval are excluded from the classifier risk set;
- complete 0062 Tier-A predictor universe, including family scores;
- multiple prespecified linear, compressed, nonlinear, hazard, and stacked classifier architectures;
- validation-only hyperparameter/model selection;
- frozen outer defensive controller family;
- no leverage, no shorting, no controller smoothing introduced for 0067;
- benchmark remains the closed 0064 passive-cash economic path;
- confirmatory multiplicity and resampling inference remain mandatory;
- exactly one authorized historical execution attempt after all lifecycle gates.

Exact event thresholds, exact data/blob identities, exact estimator grids, exact trial counts, exact maturity windows, exact support thresholds, exact controller thresholds, exact inference counts/seeds, and exact computational-qualification acceptance thresholds belong to numerical/data PREREGISTRATION after DESIGN merge.

No 0066 historical output may be consulted to choose or change those values because no complete 0066 output exists.

## 4. High-level event abstraction

### 4.1 Price-only construction

Event outcomes are constructed from future BTC or SOL close-price paths and past-only scale estimates. Predictor indicators are not read by the event constructor.

The event constructor must retain the 0066 conceptual structure:

- evaluate a fixed set of future path horizons spanning approximately 10 through 240 sessions;
- summarize future log-price path direction, terminal displacement, range, and adverse excursion;
- use past-only volatility scaling at each origin;
- identify DOWN candidates from sufficiently negative standardized path trend/displacement/adverse excursion;
- identify SIDEWAYS candidates from sufficiently neutral standardized trend/displacement/range;
- assign duration grade from the selected/qualifying horizon;
- extract unique episode onsets and suppress duplicate already-active same-type onsets;
- permit any hierarchical longer-duration exception only if prospectively frozen in preregistration.

Exact formulas and thresholds are frozen later in PREREGISTRATION and must not be inferred from historical 0067 outcomes during implementation.

### 4.2 Prediction tracks

For each asset, retain four conceptual targets:

- `ANY_DOWN`
- `MAJOR_DOWN`
- `ANY_SIDEWAYS`
- `LONG_SIDEWAYS`

Retain five conceptual warning leads:

- 1 session
- 3 sessions
- 5 sessions
- 10 sessions
- 20 sessions

A positive warning target means a qualifying unique onset occurs in a strictly future window after prediction date t, not that the onset occurs at exactly one fixed lag.

The exact track count and support gates are frozen numerically in PREREGISTRATION.

## 5. Predictor universe and architectures

### 5.1 Predictor universe

0067 is intended to reuse the complete frozen 0062 Tier-A information set rather than prune predictors using any failed-run information.

Conceptually this includes:

- all raw Tier-A signal cells admitted by 0062;
- all frozen family scores admitted by 0062;
- no post-hoc sign flip using FINAL data;
- no output-informed feature deletion based on 0066, because no complete 0066 result exists.

Exact predictor counts, feature-engine blob identities, and data identities are frozen in PREREGISTRATION.

### 5.2 Predictor architecture families

The intended architecture tournament retains the scientific breadth of 0066:

1. `FAMILY_RIDGE_LOGIT`
2. `RAW_ELASTIC_NET_LOGIT`
3. `VALIDATION_SCREENED_SIGNAL_LOGIT`
4. `PCR_LOGIT`
5. `THEORY_QUADRATIC_LOGIT`
6. `SHALLOW_GBDT_CLASSIFIER`
7. `DISCRETE_TIME_HAZARD_LOGIT`
8. `STACKED_PROBABILITY_ENSEMBLE`

The quadratic family remains explicitly RESULT_INFORMED from closed 0065, exactly as acknowledged before 0066, and receives no selection privilege.

0067 may not delete a slow architecture merely because 0066 timed out. If preregistration changes the scientific architecture set for any reason, that is a declared scientific change under 0067, not a hidden runtime optimization.

## 6. Economic controller layer

The intended controller family retains the 0066 conceptual form:

1. BTC short-lead ANY_DOWN defensive controller;
2. SOL short-lead ANY_DOWN defensive controller;
3. max BTC/SOL short-lead ANY_DOWN controller;
4. BTC medium-lead MAJOR_DOWN controller;
5. max BTC/SOL medium-lead MAJOR_DOWN controller;
6. multi-lead DOWN blend;
7. DOWN plus SIDEWAYS controller;
8. stacked event-risk controller.

Controllers reduce gross only. No leverage above the baseline path and no short exposure are introduced.

Controller probability thresholds must be determined from TRAIN/VALIDATION information only under exact preregistered rules. Evaluation/FINAL outcomes cannot tune thresholds.

Outer-turnover cost, cash treatment, benchmark reconstruction, and exact economic evaluation window are frozen in PREREGISTRATION.

## 7. The new 0067 requirement: full-shape computational qualification

### 7.1 Purpose

0067 adds a mandatory engineering qualification gate before any historical `RUN_ATTEMPT.marker` may be created.

The purpose is to establish, on nonhistorical data, that the exact implementation intended for historical execution can complete the full declared compute graph with substantial wall-clock and memory safety margin on the selected runner class.

A bounded smoke test is insufficient.

### 7.2 Historical isolation

The qualification workload must not read or materialize the historical market evidence, BRRK equity, BRRK weights, DTB3 evidence, or any other frozen historical outcome payload.

Qualification inputs must be synthetic/nonhistorical and generated independently of historical outcome values.

The qualification runner must provide auditable counters proving zero historical content reads, zero historical market-loader calls, and zero historical economic-data reads.

Git metadata/blob-identity checks are permitted if they do not read historical file content.

### 7.3 Shape equivalence

The qualification workload must reproduce the historical execution's computational shape closely enough to measure its real execution envelope. At minimum it must preserve the preregistered historical program's:

- time-axis row count or a formally equivalent/higher compute load;
- predictor dimensionality;
- number of outcome assets;
- number of event/target/lead tracks;
- architecture families;
- hyperparameter/configuration counts;
- walk-forward refit calendar and training-window geometry;
- model-fit calls implied by all validation configurations;
- final predictor-track workload;
- economic-controller workload;
- bootstrap/resampling replicate workload;
- PBO split workload where applicable;
- dependency versions;
- selected GitHub runner class / CPU architecture;
- thread and process configuration;
- random-seed discipline.

The qualification may use synthetic values, but it may not shrink the execution graph merely to pass quickly.

### 7.4 Required measurements

The qualification must persist machine-readable evidence containing at least:

- total wall-clock seconds;
- phase-level wall-clock seconds;
- peak resident memory / RSS;
- declared and actual estimator-fit counts;
- declared and actual validation-config counts;
- declared and actual final-track counts;
- declared and actual controller counts;
- declared and actual bootstrap replicate counts;
- declared and actual PBO split counts when supported;
- process/thread counts and BLAS/OpenMP thread environment;
- Python and pinned dependency versions;
- runner image / runner class identity;
- deterministic seed set;
- synthetic input identity/hash;
- implementation commit SHA;
- zero-historical-read counters;
- qualification verdict.

Exact acceptance limits and required safety factor relative to the execution timeout are numerical parameters and are frozen in PREREGISTRATION.

### 7.5 Fail-closed rule

If the full-shape qualification does not satisfy every preregistered runtime, memory, count, reproducibility, and isolation condition:

- no controlled historical execution boundary may authorize an attempt;
- no historical `RUN_ATTEMPT.marker` may be created;
- no historical data may be read for 0067 execution;
- engineering remediation may continue only within the allowed lifecycle and without historical measurement.

Passing a smaller smoke test cannot override a failed full-shape qualification.

Increasing the historical workflow timeout alone cannot substitute for qualification.

## 8. Semantic-preserving engineering optimization class

0067 explicitly separates execution engineering from scientific design.

The implementation may use optimizations only when they preserve the preregistered statistical computation and deterministic outputs, subject to equivalence tests.

Candidate semantic-preserving transformations include:

- precomputing immutable date/fold/risk-set masks;
- caching immutable feature matrices and target arrays;
- caching transformations whose fitted training sample and fitted parameters are identical to the reference computation;
- eliminating repeated pandas-to-NumPy conversions;
- vectorizing deterministic metric calculations;
- reusing immutable design matrices across hyperparameters when mathematically identical;
- vectorizing/chunking bootstrap statistic evaluation while retaining the exact preregistered resample indices and statistic;
- vectorizing PBO calculations while retaining the exact preregistered slice combinations;
- deterministic parallel execution across independent configurations or tracks;
- read-only memory maps/shared arrays to reduce duplication;
- stable result ordering after parallel execution;
- explicit CPU/thread caps to prevent oversubscription;
- phase timing and resource instrumentation.

Any cache key must include every state variable that can affect the computed object.

## 9. Changes that are scientific, not engineering

The following are not automatically allowed as runtime optimizations and require explicit scientific freezing before historical execution:

- changing event formulas, thresholds, priorities, suppression rules, or support rules;
- changing predictor membership or feature definitions;
- changing training/validation/FINAL windows or label maturity;
- changing refit cadence or deleting walk-forward refits;
- changing model family;
- changing hyperparameter grids;
- changing loss, regularization meaning, solver semantics, tolerance, convergence behavior, or randomization in a way that can alter fitted estimates;
- changing selection metrics or architecture-selection rules;
- early stopping or approximate fitting that changes estimates;
- dropping slow configurations, tracks, architectures, bootstrap replicates, or PBO splits;
- changing controller thresholds, gross mappings, costs, prediction-to-return timing, or benchmark economics;
- changing multiplicity correction, bootstrap block length, bootstrap count/seed/statistic, or PBO definition;
- using historical runtime position or partial progress to choose which scientific components survive.

If any such change is desired, it must be declared under 0067 before the relevant preregistration is merged. It may not be introduced after historical access as a rescue.

## 10. Deterministic parallelism and equivalence

Parallelism is permitted only across computations that are scientifically independent conditional on frozen inputs/configuration.

The implementation must freeze deterministic seeding and stable reduction/order rules so that scheduling order does not determine the output.

Before the full-shape qualification gate, implementation tests must compare a serial/reference path and the optimized path on nonhistorical fixtures. Exact-equality or prospectively specified numerical-equivalence tolerances must be defined in PREREGISTRATION where floating-point ordering can legitimately differ.

A performance improvement is acceptable only if scientific output equivalence passes.

## 11. Reference implementation philosophy

0067 should preserve a small, auditable reference path for semantic comparison even if the production-shaped qualification path is optimized.

The reference path does not need to be fast enough for the full historical run, but it must be able to validate representative nonhistorical cases for:

- event labels/onsets;
- risk-set masks;
- training maturity masks;
- transformed predictors;
- fitted probabilities for representative configurations;
- validation metrics;
- controller gross paths;
- turnover costs;
- bootstrap/PBO statistics for representative fixtures.

The optimized path must not silently become the only definition of correctness.

## 12. Runtime observability

The historical implementation must emit progress without exposing or enabling post-hoc scientific decision-making.

At minimum observability should identify:

- current scientific phase;
- completed/declared config counts;
- completed/declared fit counts;
- elapsed time by phase;
- memory high-water mark where available.

Progress logging is diagnostic only. It cannot authorize cancellation-and-retry after a historical attempt marker is consumed.

No partial scientific result should be used for design changes after marker creation.

## 13. Exactly-once execution architecture

0067 retains the fail-closed exactly-once architecture:

1. DESIGN merged;
2. numerical/data PREREGISTRATION merged;
3. IMPLEMENTATION-ONLY merged;
4. full-shape nonhistorical computational qualification passed on the exact intended implementation/environment;
5. CONTROLLED-EXECUTION BOUNDARY merged;
6. exact merged-boundary Git-only preflight with zero historical content reads;
7. unique `RUN_ATTEMPT.marker` durably persisted before historical content read;
8. historical inputs read under the frozen interface;
9. scientific engine invoked exactly once;
10. immutable result/evidence/execution bundle persisted create-only;
11. `RUN_ONCE.marker` finalized without historical remeasurement;
12. immutable CLOSEOUT.

The exact placement and hash binding of the qualification artifact to implementation/boundary are frozen in PREREGISTRATION/implementation contract.

Once a 0067 historical `RUN_ATTEMPT.marker` exists, the same no-rerun/no-retune/no-rescue rule applies even if infrastructure subsequently fails.

## 14. Qualification evidence is not scientific evidence

Synthetic qualification may be repeated during implementation engineering because it contains no historical scientific outcome information. Repetition is allowed for performance engineering until the implementation and acceptance procedure are frozen.

Qualification timing/memory results cannot be used to alter scientific hypotheses except through an explicit pre-historical lifecycle change.

The final qualification pass used to authorize a boundary must be bound to the exact implementation/environment that will be used historically. Earlier exploratory performance runs do not substitute for that final bound qualification.

## 15. Data and evidence policy

No 0067 historical data measurement occurs in DESIGN.

PREREGISTRATION will pin exact historical data/blob identities before implementation. The intended data lineage remains the same frozen family used by 0066 unless a data change is explicitly declared prospectively.

Network fetch is intended to remain forbidden for the historical run. Replacement data are intended to remain forbidden.

The historical input reader must retain read-count instrumentation so execution evidence can show exactly which frozen inputs were read and how many times.

## 16. Inference and multiplicity policy

0067 retains separate inferential families conceptually:

- broad signal/event atlas multiplicity control;
- predictor/controller confirmatory resampling inference;
- model-selection dependence/overfit diagnostic such as PBO where support permits.

Exact test statistics, multiplicity family, correction method, block bootstrap geometry, replicate count, seed, confidence bounds, PBO slices, and support gates are frozen in PREREGISTRATION.

Runtime optimization may change only how those exact computations are executed, not what inferential question they answer.

## 17. Success and failure interpretation

0067 scientific classification will be prospectively frozen in RESULT_SCHEMA/PREREGISTRATION.

At DESIGN level:

- scientific PASS requires robust event-warning evidence and/or a robust economically useful controller under the frozen hierarchy;
- scientific FAIL means the successfully executed frozen program does not meet its scientific gates;
- insufficient supported events may produce a measurement-inconclusive classification;
- execution/contract failure must be separated from scientific rejection.

A compute-qualification failure before historical access is an engineering gate failure, not a scientific result and not a consumed historical attempt.

An infrastructure failure after `RUN_ATTEMPT.marker` is consumed remains an execution invalidation; it does not become a scientific FAIL.

## 18. No hidden rescue through infrastructure

The following are explicitly forbidden:

- reading historical data first and deciding afterward whether the run counts;
- running several historical shards/attempts and selecting the first complete one unless such sharding is frozen as one atomic exactly-once execution before access;
- retrying a failed historical config subset after marker creation;
- rerunning only failed architectures;
- recomputing a missing result bundle from historical data after an incomplete attempt;
- using partial logs to prune the remaining workload;
- silently increasing approximation or loosening convergence to finish before timeout;
- treating infrastructure retries as scientifically free after historical access.

Any distributed/sharded execution design must be prospectively defined as one atomic historical attempt with deterministic aggregation and failure semantics before the marker is crossed.

## 19. Production authority

`production_authorized = false`
`signature_authorized = false`
`order_submission_authorized = false`
`production_authorized_components = []`

Canonical BRRK-0011: NO CHANGE.
Closed 0064: NO CHANGE.
Closed 0065: NO CHANGE.
Closed 0066: NO CHANGE.
Phase 6: NO CHANGE.

## 20. DESIGN-stage zero-result assertions

At DESIGN merge time all of the following must remain true for 0067:

- historical event atlas computed = false;
- historical indicator/event association computed = false;
- historical classifier metrics computed = false;
- historical controller economics computed = false;
- historical tuning configurations evaluated = 0;
- historical model/controller variants evaluated = 0;
- `RUN_ATTEMPT.marker` = ABSENT;
- `PRIMARY_RESULT.json` = ABSENT;
- `EVIDENCE.json` = ABSENT;
- `EXECUTION.json` = ABSENT;
- `RUN_ONCE.marker` = ABSENT.

Synthetic/nonhistorical performance fixtures, when later created, are not historical result artifacts and must be clearly namespaced as qualification evidence.

## 21. Exact next lifecycle step

Merge this DESIGN only after fresh standing CI.

After DESIGN merge, create numerical/data PREREGISTRATION for 0067. PREREGISTRATION must freeze, before any implementation or historical measurement:

- exact data and code/blob identities;
- exact event formulas/thresholds and onset/suppression logic;
- exact track/support counts;
- exact predictor universe/counts;
- exact estimator grids/solver semantics/seeds;
- exact validation tuning and final variant accounting;
- exact windows/refit/maturity rules;
- exact controller mappings and costs;
- exact inference/multiplicity/PBO procedures;
- exact synthetic full-shape qualification generator/shape;
- exact runtime and peak-memory acceptance thresholds;
- exact safety margin relative to the selected runner hard timeout;
- exact runner/dependency/thread/process environment;
- exact equivalence tests/tolerances;
- exact qualification evidence schema and hash binding;
- exact historical result/evidence/execution schemas;
- exactly-once persistence and recovery semantics.

No implementation-only optimization and no 0067 historical measurement is authorized by this DESIGN document alone.
