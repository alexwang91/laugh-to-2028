# 0071 preflight contamination governance resolution

Status: `PROSPECTIVE GOVERNANCE AMENDMENT / NO RETROACTIVE VALIDATION`

Date: 2026-08-18

Authoritative repository: `alexwang91/laugh-to-2028`

Incident record: GitHub issue #295.

## 1. Scope

This governance amendment addresses only the pre-attempt controlled-content exposure that occurred for `BRRK-SOL-LONG-SIDEWAYS-CONTROLLER-INTEGRATION-0071` after merged CONTROLLED BOUNDARY `0cad34af6ea5919f974a593b3a0f9427a5c1c5fd` and before any durable 0071 `RUN_ATTEMPT.marker` existed.

It does not reinterpret the prior read as legal, does not exempt connector/UI/API content access from controlled-read accounting, does not create a retroactive marker, and does not authorize any further 0071 scientific action.

## 2. Permanent treatment of 0071

0071 remains formally complete only through Stage 6 CONTROLLED BOUNDARY, i.e. `6/10`.

Stage 7 ZERO-RESULT PREFLIGHT is permanently classified `BLOCKED_PRE_ATTEMPT_CONTROLLED_CONTENT_CONTAMINATION`.

The following are permanently forbidden for 0071:

- claiming Stage 7 PASS;
- creating a 0071 `RUN_ATTEMPT.marker` after the exposure;
- executing Stage 8 under 0071;
- reading any additional controlled scientific/economic payload under 0071;
- producing 0071 `PRIMARY_RESULT`, `EVIDENCE`, `EXECUTION`, `RUN_ONCE` or scientific classification artifacts;
- fabricating Stage 8, RESULT, or IMMUTABLE CLOSEOUT completion;
- changing any frozen 0071 scientific parameter, cost, gate, candidate, source identity, or production authority.

0071 attempt state remains `0/1`, unconsumed. This is an incomplete terminally blocked lifecycle, not a scientific FAIL/INCONCLUSIVE/INVALID result and not an immutable closeout.

## 3. Prospective replacement ID

A new replacement research ID is authorized only after this governance amendment merges:

`BRRK-SOL-LONG-SIDEWAYS-CONTROLLER-INTEGRATION-REPLACEMENT-0083`.

0083 is not allowed to use the contamination event to change the scientific question or improve the experiment. Its OWNER-FIRST must prospectively bind the replacement to the already-frozen 0071 scientific contract and explicitly prohibit result-informed modifications.

The replacement must independently traverse all ten lifecycle stages:

`OWNER-FIRST -> DESIGN -> PREREGISTRATION -> IMPLEMENTATION -> NONHISTORICAL QUALIFICATION -> CONTROLLED BOUNDARY -> ZERO-RESULT PREFLIGHT -> ONE CONTROLLED DEVELOPMENT-HISTORY ATTEMPT -> RESULT -> IMMUTABLE CLOSEOUT`.

No stage may be inherited as formally completed from 0071. However, DESIGN/PREREGISTRATION may cite and mechanically reproduce the frozen 0071 semantics only if they make no scientific change. Any proposed scientific change requires a different new research ID and fresh prospective justification.

0083 must use a new result branch, new durable attempt marker, new create-only result artifact chain and its own exactly-once accounting. It may not treat the accidental 0071 connector read as one of its controlled reads. Before the 0083 attempt, all 0083-controlled content reads must be zero under its own accounting, and the first permitted 0083 controlled read must follow durable 0083 marker persistence.

## 4. Downstream dependency amendment

The original program map lists 0072 hard prerequisite as `0071 closeout`. Because 0071 cannot lawfully reach closeout without fabricating skipped lifecycle stages, this amendment prospectively replaces that dependency as follows:

- 0072 must not start from 0071.
- 0072 hard prerequisite becomes `0083 immutable closeout`.
- If 0083 reaches a terminal non-PASS closeout, 0072 may start only if the original 0072 purpose does not require a PASS result from the controller-integration study; any use of controller-specific findings remains prohibited unless supported by the actual 0083 terminal result.
- 0073 and later dependencies remain as written in `CRYPTO_MULTI_STRATEGY_RESEARCH_PROGRAM_0071_0082.md` unless a later governance amendment explicitly changes them.

This dependency change is prospective from the merge of this amendment. It does not make the prior 0071 preflight exposure compliant.

## 5. Frozen lineage and anti-rescue rule

0083 must preserve the already-frozen lineage unless a separate new-ID scientific amendment is prospectively approved before any 0083 controlled read:

- SOL-only controller-integration question;
- locked 0070 `P02_RAW_ELASTIC_NET_LOGIT|SOL|T4_LONG_SIDEWAYS`, lead 10, sole predictive warning input;
- exactly six selectable controller families: `BINARY_RISK_OFF`, `LINEAR_DERISK`, `PIECEWISE_DERISK`, `VOL_ADJUSTED`, `DRAWDOWN_AWARE`, `HYSTERESIS`;
- matched signal-off controls for volatility and drawdown overlays;
- long-or-cash only, no leverage and no short;
- frozen 0064 passive-cash semantics;
- C0/C1/C2 framework and all numerical gates only as already frozen by merged 0071 PREREGISTRATION `34ccffa6ecedffb5076b88cf86222c2fb142560c` unless a separate new-ID scientific amendment is created before 0083 OWNER-FIRST merge.

No observed information from the accidental 0069 content exposure may be used to retune, narrow, widen, reprioritize or rescue the six-controller test.

## 6. Production authority

No change:

- `production_authorized=false`
- `signature_authorized=false`
- `order_submission_authorized=false`

Canonical BRRK-0011 and all closed 0064-0070 results remain unchanged.

## 7. Exact next step

After this governance amendment passes exact-head standing CI and merges, create a separate OWNER-FIRST PR for `BRRK-SOL-LONG-SIDEWAYS-CONTROLLER-INTEGRATION-REPLACEMENT-0083`. Do not read any additional controlled scientific/economic payload before the 0083 lifecycle reaches its own merged boundary, identity-only zero-result preflight PASS, and durable 0083 attempt marker.