# Resolution invariants

This directory-level governance addition is intentionally science-free.

It records that 0071 remains incomplete at 6/10 with attempt 0/1, permanently blocks same-ID continuation after pre-attempt controlled-content exposure, and authorizes only a new fully governed replacement ID after this amendment itself merges.

It does not alter any frozen controller formula, threshold, cost, cash rule, candidate count, source identity, result artifact, production authority, or any immutable 0064-0070 result.

The exact immutable CURRENT_STATE line `workflow run                         31381953131 / attempt 1` is not modified by this governance-only branch.
