[project]
name = "crypto-beta-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "hyperliquid-python-sdk==0.24.0",
  "requests==2.32.4",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
