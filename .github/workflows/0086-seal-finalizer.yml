name: 0086 seal finalizer

on:
  push:
    branches:
      - research/0086-factor-atlas-result-v1
    paths:
      - .github/workflows/0086-seal-finalizer.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/0086-factor-atlas-result-v1
          fetch-depth: 0
      - name: Append immutable CURRENT_STATE handoff and closeout
        shell: bash
        run: |
          set -euo pipefail
          cat >> docs/CURRENT_STATE.md <<'EOF'

## 0086 Factor Atlas immutable PASS seal — 2026-08-26

- `BRRK-CROSS-SECTIONAL-FACTOR-ATLAS-0086` completed its single authorized controlled attempt `1/1` through `ControlledResearchRunnerV1SourceQualified`; durable `RUN_ATTEMPT.marker` preceded all controlled payload reads.
- The run consumed exactly `15,254` controlled source reads and exactly `1/1` scientific engine invocation. Source-network fetches remained `0`. `PRIMARY_RESULT.json` and `RUN_ONCE.marker` were create-only persisted on `research/0086-factor-atlas-result-v1`.
- Persisted execution classification is `EXECUTION_VALID`; scientific classification is `PASS_VALIDATED_FACTOR_ATLAS`; `scientific_result_admissible=true`.
- All three frozen factor candidates pass G0-G9: `MOM60_RAW` persisted sign `-1`, `RVOL20_RAW` persisted sign `-1`, `LIQ30_RAW` persisted sign `+1`. No post-result factor reselection, sign change, retune, rescue, rerun or recomputation is permitted.
- Mean weekly rank ICs are MOM60 `-0.05754786555676433`, RVOL20 `-0.12389264424859531`, LIQ30 `0.037890696622620984`; corresponding frozen 20 bps net aligned weekly spreads are `0.004849416287787996`, `0.009230759613991876`, and `0.0021777487441069694`.
- Evidence tier remains researcher-exposed DEVELOPMENT history, not independent OOS. PASS unlocks a separately governed Factor L/S research ID only; it grants no production/signature/order/withdrawal/transfer authority.
- 0085 remains immutable `INVALID_EXECUTION`; 0076 remains sealed at its Stage7 pre-marker incident; 0072/0073 remain paused; 0083 remains immutable FAIL; Phase6 closeout and all CAPTURE anchors remain unchanged.
EOF
          cat > research/brrk_cross_sectional_factor_atlas_0086/CLOSEOUT.md <<'EOF'
# 0086 Factor Atlas Immutable Closeout

Research ID: `BRRK-CROSS-SECTIONAL-FACTOR-ATLAS-0086`

Terminal state: `PASS_VALIDATED_FACTOR_ATLAS / CLOSED TO SAME-ID RERUN`

The unique authorized controlled attempt `1/1` completed under `ControlledResearchRunnerV1SourceQualified`. A durable `RUN_ATTEMPT.marker` preceded all controlled payload reads. The runner consumed exactly 15,254 source reads, invoked the frozen scientific engine exactly once, persisted `PRIMARY_RESULT.json` create-only, and sealed the attempt with `RUN_ONCE.marker`.

Persisted execution classification is `EXECUTION_VALID` and `scientific_result_admissible=true`. All three frozen candidates pass G0-G9. Persisted factor signs are `MOM60_RAW=-1`, `RVOL20_RAW=-1`, and `LIQ30_RAW=+1`.

This PASS qualifies only a separately governed future Factor L/S research ID. Same-ID rerun, retune, rescue, recomputation, source substitution, factor reselection, and sign reinterpretation are forbidden. Evidence remains researcher-exposed DEVELOPMENT history and grants zero production/signature/order/withdrawal/transfer authority.

## What did not change

0085 remains immutable `INVALID_EXECUTION`; 0076 remains sealed at its Stage7 pre-marker incident; 0072/0073 remain paused; 0083 remains immutable FAIL. `workflow run                         31381953131 / attempt 1`, CAPTURE-0001 sealed/no-retry, CAPTURE-0002 permanently claimed/no-refetch, Phase6 closeout, frozen source identities, and common-runner governance remain unchanged.
EOF
          rm .github/workflows/0086-seal-finalizer.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/CURRENT_STATE.md research/brrk_cross_sectional_factor_atlas_0086/CLOSEOUT.md .github/workflows/0086-seal-finalizer.yml
          git commit -m "research(0086): seal validated factor atlas"
          git push origin HEAD:research/0086-factor-atlas-result-v1
