name: 0088 seal CURRENT_STATE handoff

on:
  push:
    branches:
      - "research/0088-factor-ls-result-v1"
    paths:
      - ".github/workflows/0088-seal-current-state-handoff.yml"

permissions:
  contents: write

jobs:
  append-and-self-delete:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: "research/0088-factor-ls-result-v1"
          fetch-depth: 0
      - name: Append immutable 0088 SEAL handoff and self-delete
        run: |
          set -euo pipefail
          cat >> docs/CURRENT_STATE.md <<'EOF'

## 0088 Factor L/S immutable FAIL seal — 2026-08-27

- `BRRK-FACTOR-LS-0088` completed its unique authorized controlled attempt `1/1` in GitHub Actions workflow `33028536813` through `CONTROLLED_RESEARCH_RUNNER_V1` / `ControlledResearchRunnerV1SourceQualified`.
- Execution classification is `EXECUTION_VALID`; `scientific_result_admissible=true`; durable `RUN_ATTEMPT.marker` preceded controlled payload reads; exactly `30,336` controlled objects were read and the scientific engine was invoked exactly `1/1`; `PRIMARY_RESULT.json` and `RUN_ONCE.marker` were persisted create-only.
- Persisted scientific classification is `FAIL_FACTOR_LS_GATES / CLOSED TO SAME-ID RERUN`. G0/G1/G5/G7 PASS; G2 net return, G3 Sharpe, G4 drawdown, G6 chronology, G8 state/LOYO and G9 implementation FAIL.
- Key frozen-result diagnostics: annualized Sharpe `0.09891513515488597`; max drawdown `-0.7439030735108538`; positive-week fraction `0.556420233463035`; observations `257`; mean C0/C1/C2 `0.0027521631674629413` / `0.0017770658911983495` / `0.0008019686149337581`.
- Frozen source identity remains artifact `9495175701`, digest `sha256:8040282ff412b2d3fd360173e4745ebfd048796eb9e9c2ad49fa0901e5cedf56`, manifest `sha256:c33b575cc436db795086458a25ca38fe1527f649809e549caba00e9754422e58#0088-usdm-kline-funding-30336`.
- Same-ID rerun, retune, rescue, recomputation, factor reselection, sign reinterpretation, threshold relaxation, source substitution and history extension are forbidden. Immutable 0086 `PASS_VALIDATED_FACTOR_ATLAS` and its signs remain unchanged.
- 0085 remains immutable `INVALID_EXECUTION`; 0076 remains sealed at its Stage7 pre-marker incident; 0072/0073 remain paused; 0083 remains immutable FAIL; 0087 remains `BLOCKED_NO_QUALIFYING_CONTROLLED_SOURCE_METADATA`; Phase6 closeout and CAPTURE anchors remain unchanged.
- `workflow run                         31381953131 / attempt 1`, CAPTURE-0001 sealed/no-retry and CAPTURE-0002 permanently claimed/no-refetch remain immutable. Production/signature/order/withdrawal/transfer authority remains false.
EOF
          rm .github/workflows/0088-seal-current-state-handoff.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/CURRENT_STATE.md .github/workflows/0088-seal-current-state-handoff.yml
          git commit -m "docs(0088): record immutable Factor L/S FAIL handoff"
          git push origin HEAD:research/0088-factor-ls-result-v1
